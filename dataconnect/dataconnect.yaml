specVersion: "v1beta"
serviceId: "events"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "events-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
